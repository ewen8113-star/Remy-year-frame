# 人头马年框项目管理系统代码开发规范

本文件是 Cursor 及其他代码助手维护本项目时的首要执行规范。开始修改前必须完整阅读本文件、根目录 `.cursorrules`，以及目标目录中的现有实现。

## 1. 基本原则

1. 保持业务行为不变，先理解现有数据流再修改。
2. 只修改需求直接涉及的代码，不顺带重构相邻稳定模块。
3. 优先小范围、可验证、可回退的改动。
4. 不猜测数据库结构、接口字段或业务枚举；先从代码、迁移和实际响应确认。
5. 不删除历史数据、业务字段、迁移或备份文件，除非用户明确授权。
6. 不引入新依赖、框架、ORM、TypeScript 或构建工具，除非用户明确授权。
7. 函数原则上少于 50 行，源代码文件原则上少于 300 行。
8. 保持当前命名和格式风格，不对无关文件做格式化。

## 2. 技术栈边界

- Runtime：Node.js 20 LTS 兼容语法。
- Backend：Express 4、CommonJS、mysql2。
- Frontend：原生 HTML、CSS、经典脚本 JavaScript。
- Database：MySQL 8，手写参数化 SQL。
- Authentication：express-session Cookie 会话。
- Deployment：PM2；本地默认端口为 `3088`。

禁止在未获授权时引入 React、Vue、Angular、Svelte、jQuery、Tailwind、ORM、ES Modules 或前端打包流程。

## 3. 当前目录职责

```text
src/server.js              Express 启动、全局中间件和路由挂载
src/routes/                各业务域的轻量路由入口
src/<domain>/              业务域子路由、辅助函数和导出逻辑
src/config/                数据库等基础配置
src/middleware/            认证和权限中间件
src/lib/                   可跨业务域复用的纯工具
src/scripts/               幂等迁移、修复和一次性运维脚本
public/index.html           SPA 外壳、样式及经典脚本加载顺序
public/app*.js             全局应用启动和导航逻辑
public/modules/            前端业务页面及拆分文件
public/styles/             已拆分的样式文件
public/style.css            旧兼容入口，不再存放实际样式
scripts/                   静态检查脚本
docs/                      维护说明和优化记录
```

新增代码应放入现有业务域，不要重新堆回大型路由入口或单一前端文件。

## 4. 后端规范

### 4.1 模块与路由

- 使用 `require()` 和 `module.exports`。
- `src/routes/<domain>.js` 只负责创建 Router、挂载子路由和导出。
- 复杂 handler、SQL、文件导出和业务辅助函数放在 `src/<domain>/`。
- 新业务域必须在 `src/server.js` 中明确挂载。
- 保持现有 URL 和请求/响应字段兼容。
- API 成功响应沿用当前路由格式；失败响应至少包含可读的 `error`。
- 异步 handler 必须捕获异常并返回明确 HTTP 状态码。

### 4.2 认证与权限

- `/api/health` 和 `/api/auth/*` 之外的接口默认需要登录。
- 写请求受 `requireWriteAccess` 保护，不能绕过权限中间件。
- `operator` 可登记报销（含导入/合并及个人收款方回写）；付款单、库存主数据、用户与字典管理等写操作仍仅 `admin`。
- 管理员专属操作继续使用现有角色检查。
- 不修改 Session Cookie 的 `httpOnly`、`sameSite`、生产环境 `secure` 语义。
- 不在日志、响应或前端代码中输出密码、哈希、Session ID、数据库密码。

### 4.3 数据库与 SQL

- 连接统一使用 `src/config/database.js`。
- 所有用户输入必须通过 `?` 占位符参数化，禁止拼接进 SQL。
- 明确列名，避免依赖 `SELECT *` 的隐式字段顺序。
- 写入前校验必填值、枚举、数字范围和关联记录。
- 多步写操作需要原子性时使用事务，并确保异常时回滚。
- 不改变现有字段含义；涉及金额时保持当前含税、未税和成本口径。
- 日期及时区逻辑优先复用 `src/lib/businessTime.js` 等已有工具。

### 4.4 数据库变更

- 新增或修改结构必须提供 `src/scripts/` 下的幂等迁移脚本。
- 迁移脚本命名使用明确动词和对象，例如 `addInventoryFooColumn.js`。
- 在 `package.json` 注册对应的 `migrate:<name>` 命令。
- 迁移前确认表、列和索引是否存在；重复执行不得破坏数据。
- 数据修复脚本必须限定范围、支持预览或输出影响行数。
- 未经明确授权，不自动执行生产写入、恢复、批量修复或删除脚本。

## 5. 前端规范

### 5.1 经典脚本与加载顺序

- 当前前端不是 ES Module；拆分文件通过全局作用域共享函数和状态。
- `public/index.html` 中 `<script>` 的先后顺序就是依赖顺序，不能随意排序。
- 基础工具先于页面模块，主文件先于对应的 `part-N` 文件。
- 新增拆分文件后，必须同步加入 `public/index.html`，并确认文件存在。
- 不在子文件中重复声明同名顶层 `const` 或 `let`。
- 跨文件依赖应保持清晰：定义文件必须先加载，使用文件后加载。
- 单个大函数不能仅靠移动到新文件规避长度限制，应提取有明确职责的辅助函数。

### 5.2 页面渲染与 API

- 页面切换继续通过 `navigate(page)` 和现有 render 函数体系实现。
- API 请求优先使用 `public/modules/api-client.js` 中的公共能力。
- 请求必须保持同源凭证，正确处理 401、403 和服务器错误。
- 用户可见错误使用现有 Toast、空状态或错误提示组件。
- 渲染动态内容时使用现有转义工具，禁止把不可信值直接插入 HTML。
- 不改变核心业务操作的入口、字段含义、权限和确认流程。

### 5.3 CSS

- 全局变量和基础规则放在 `public/styles/base.css`。
- 通用组件放在 `public/styles/components.css`。
- 布局规则放在 `public/styles/layout.css`。
- 登录与注册页放在 `public/styles/auth.css`。
- 业务页面样式放入对应的 `public/styles/<domain>.css`。
- `public/style.css` 仅为旧兼容入口，不向其中追加新样式。
- 新页面需要在 HTML 中显式加载所依赖的拆分 CSS。
- 使用现有 CSS 变量和 HarmonyOS UI 规范，避免无语义的魔法颜色。
- 必须覆盖必要的 hover、focus、pressed、disabled 和 error 状态。
- 修改后检查暗色、亮色以及常见窄屏宽度。

### 5.4 HTML 与可访问性

- 交互元素使用正确的 `button`、`a`、`input`、`label` 语义。
- 图标按钮必须提供 `title` 或 `aria-label`。
- 表单控件应关联可读标签；必填、禁用和错误状态要可感知。
- 弹窗必须保留关闭和取消路径，不得用页面重绘破坏全局弹窗 DOM。

## 6. 业务保护线

- 年框、项目编号、品牌、区域、归属和活动状态是跨模块关联关键字段。
- 活动成本、仓储、物流、统筹、报销和付款单之间不得重复计入。
- 报价、成本、税费、服务费和利润口径必须沿用现有算法。
- 库存出库、入库、归还、酒品统计和物流同步必须保持可追溯。
- 付款申请、付款单、临时对账和正式写入流程不得绕过确认步骤。
- 导入、恢复、批量更新、删除、状态切换必须保留预览或二次确认。
- 修改任一关联字段前，先搜索其在前端、路由、SQL、导出和统计中的全部使用位置。

## 7. 文件拆分规则

- 文件接近 300 行时，按业务职责拆分，不按任意行号截断。
- 后端优先拆为 `readRoutes`、`createRoutes`、`updateRoutes`、`actionRoutes`、`routeHelpers`。
- 前端拆分时保持现有 `*-part-N.js` 约定和严格加载顺序。
- 提取的辅助函数应尽量纯粹，避免引入新的隐式全局变量。
- 拆分后必须检查导入、导出、闭包变量、上传配置及常量依赖。
- 不允许通过压缩多条语句到一行来满足行数限制。

## 8. 修改流程

### 修改前

1. 阅读本规范、`.cursorrules` 和目标模块。
2. 执行 `git status --short`，识别并保护用户已有改动。
3. 执行 `npm run check:js`，记录基线。
4. 搜索目标字段、函数和 API 的所有调用方。
5. 对数据库写入、删除、恢复或部署操作确认授权和影响范围。

### 修改中

1. 每次只解决一个明确问题。
2. 保持接口、DOM ID、全局函数名和脚本加载顺序兼容。
3. 不覆盖或清理与任务无关的未提交改动。
4. 关键分支写简洁注释，避免解释显而易见的代码。
5. 发现需求外问题时先记录，不擅自扩大修改范围。

### 修改后

至少执行：

```bash
npm run check:js
git diff --check
```

还必须按改动类型验证：

- HTML/前端拆分：检查所有本地 CSS/JS 引用存在且无重复。
- UI 修改：真实浏览器验证目标页、暗色/亮色、主要交互和控制台。
- 路由修改：加载路由模块，并验证相关 GET/错误路径。
- 数据库修改：在安全环境验证迁移可重复执行并检查影响行数。
- 导出修改：实际生成 Excel/PDF/JSON，并打开检查内容。
- 认证修改：验证未登录、普通用户、管理员三种权限路径。

项目当前没有通用 `test` 或 `build` 脚本，不得把缺失脚本声称为已通过。若新增自动化测试，应同步注册标准 npm 命令。

## 9. 静态资源检查

修改 `public/index.html`、登录页、注册页或拆分文件后，必须确认：

1. 每个本地 `src`、`href` 文件真实存在。
2. 脚本没有重复引用。
3. 依赖定义早于使用。
4. 登录和注册页至少加载 `base.css`、`components.css`、`auth.css`。
5. 页面不会因 401 以外的异常跳回登录页。
6. 主页面所有导航项可以完成渲染。

## 10. Git 与交付

- 未经用户明确要求，不执行 `git add`、提交、推送、打标签或部署。
- 不使用 `git reset --hard`、强制覆盖或删除用户改动。
- 提交应小而聚焦，提交信息说明业务影响。
- 交付说明必须列出修改文件、验证命令、验证结果和未覆盖风险。
- 如果浏览器、数据库或凭证阻塞验证，必须明确说明，不能宣称全部通过。

## 11. 完成清单

- [ ] 需求范围没有扩大。
- [ ] 业务逻辑和权限保持兼容。
- [ ] SQL 全部参数化。
- [ ] 文件和函数长度符合规范。
- [ ] 新拆分文件已按顺序加载。
- [ ] 登录、注册和相关业务页样式正常。
- [ ] `npm run check:js` 通过。
- [ ] `git diff --check` 通过。
- [ ] 真实页面或相关 API 已回归。
- [ ] 没有修改、删除或提交无关文件。
- [ ] 未验证项目和已知风险已在交付说明中列出。
